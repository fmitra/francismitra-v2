import { h } from 'preact';

const Info = (props) => (
  <div className="page info">
    <span>My name is Francis.</span>
    <span>I make things. Sometimes with code.</span>
    <span>Sometimes with photos.</span>
  </div>
);

export default Info;
